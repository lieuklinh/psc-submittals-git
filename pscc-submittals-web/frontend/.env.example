# Leave empty for local dev — Vite's dev server proxies /api to the backend
# (see vite.config.ts). Set this only for a production build that talks to
# a separately-hosted backend, e.g.:
# VITE_API_URL=https://pscc-submittals-api.azurewebsites.net
VITE_API_URL=
